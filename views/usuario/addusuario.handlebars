{{#each erros}}

<div class="alert alert-danger"> {{texto}} </div>

{{else}}

{{/each}}
<h3>Nova categoria: </h3>
<div class="card">
  <div class="card-body">
    <form action="/admin/registro/nova" method="post">
        <div class="mb-3">
            <label for="nome">Nome</label>
            <input type="text" class="form-control" name="nome" placeholder="Seu nome completo">
        </div>
        <div class="mb-3">
            <label for="slug">E-mail</label>
            <input type="email" class="form-control" name="email" placeholder="E-mail de contato">
        </div>
        <hr>
        <div class="mb-3">
            <label for="slug">Senha</label>
            <input type="password" class="form-control" name="senha" placeholder="">
        </div>
        <div class="mb-3">
            <label for="slug">Repetir a senha</label>
            <input type="password" class="form-control" name="senha2" placeholder="">
        </div>

        <button type="submit" class="btn btn-success mt-2">Cadastrar</button>
    </form>
  </div>
</div>